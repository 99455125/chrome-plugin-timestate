# 插件问题排查手册

这份手册的目标很直接：

当这个插件在某台电脑上报错时，你可以快速判断问题出在：

1. popup
2. background service worker
3. content script
4. 业务页面
5. iframe 内的“我的日报”页面

如果你能先把出错层级定位对，后面的修复就会快很多。

## 1. 先记住一个核心原则

这个插件不是单一页面程序，而是多上下文程序。

它同时运行在：

1. popup 页面
2. background service worker
3. content script
4. 业务系统顶层页面
5. 业务系统 iframe 页面

所以很多 bug 的根本原因不是“代码不会执行”，而是“代码在错误的上下文里执行”。

## 2. 排查顺序

建议永远按这个顺序排查，不要乱跳。

1. 先看插件是否已正确重载
2. 再看 popup 是否报错
3. 再看 background service worker
4. 再看业务页 Console
5. 再看 iframe 页面状态
6. 最后再看网络请求

## 3. 第一步：先确认是不是旧脚本

这是最常见的假问题。

你明明已经改了代码，但页面里跑的还是旧的 content script。

正确操作：

1. 打开 `chrome://extensions`
2. 找到插件“日报工时统计”
3. 点击“重新加载”
4. 回到业务系统主页
5. 手动刷新页面
6. 再重新测试

如果你只重载插件、不刷新业务页，页面里通常还保留旧 content script。

## 4. 第二步：看 popup 报什么错

popup 是你点击浏览器插件图标后看到的小面板。

### 怎么打开 popup 调试

1. 打开 `chrome://extensions`
2. 打开开发者模式
3. 找到这个插件
4. 点击 popup 对应的 inspect 入口

### popup 负责什么

- 接收用户点击
- 发起统计
- 显示结果
- 打开悬浮窗

### 如果 popup 出问题，常见表现

- 点按钮没反应
- 状态文字不更新
- 结果区一直不变
- 一点按钮就立刻报错

### popup 层常见判断

如果 popup 都没拿到活动 tab，通常会报：

- `未找到当前标签页。`

这类问题一般和浏览器本身、标签页状态、权限有关，不是业务页面逻辑问题。

## 5. 第三步：看 background service worker

这是最重要的调试点之一。

### 怎么打开

1. 打开 `chrome://extensions`
2. 找到插件
3. 点击 `service worker`

### background 负责什么

- 接收 `RUN_STATISTICS`
- 计算日期范围
- 注入 `content.js`
- 给 tab 发消息
- 解析 HTML
- 保存最近一次统计结果

### 这里最常见的问题

#### 5.1 页面通信失败

错误类似：

- `页面通信失败: ...`
- `Receiving end does not exist`

含义：

- background 给页面发消息时，页面里没有可接收消息的 content script

通常原因：

1. 插件刚更新，但页面没刷新
2. content script 没成功注入
3. 当前 tab 不是业务页面

优先处理：

1. 重载插件
2. 刷新业务页
3. 重新测试

#### 5.2 注入页面脚本失败

错误类似：

- `注入页面脚本失败: ...`
- `当前页面不允许注入脚本，请切到考勤系统主页面后重试。`
- `当前标签页不是业务系统页面，请切到考勤系统主页面后重试。`

通常原因：

1. 当前标签页不是内网页面
2. 当前标签页是 Chrome 自己的页面，比如 `chrome://`
3. host 权限不匹配

### background 层最关键的检查点

看这几个问题：

1. `RUN_STATISTICS` 有没有收到
2. `executeScript` 有没有报错
3. `tabs.sendMessage` 有没有报错
4. 解析器有没有抛错

## 6. 第四步：看业务页面 Console

打开业务系统主页的 F12，看 Console。

### 第一条必看日志

如果 content script 成功跑起来，会输出：

```text
[work-hours-stats] content script ready
```

对应代码在 [content.js](/Users/piaochongmogu/VscodeProjects/chrome-plugin/content.js#L7)。

### 如果看不到这条日志

优先怀疑：

1. 页面没刷新
2. 插件没重载
3. `content.js` 没注入
4. 当前页面不在 `manifest.json` 的匹配范围里

也就是先看：

- [manifest.json](/Users/piaochongmogu/VscodeProjects/chrome-plugin/manifest.json#L18)

里的 `content_scripts.matches` 和 `host_permissions` 是否覆盖当前业务域名。

## 7. 第五步：手动检查页面结构

很多问题不是插件坏了，而是页面结构和样本不一样了。

在业务页 Console 里，优先执行这些命令。

### 7.1 看当前页面地址

```js
location.href
```

先确认你是不是在业务系统主页。

### 7.2 看有没有主 iframe

```js
document.querySelector("#mainframe")
```

如果返回 `null`，说明：

1. 页面结构变了
2. 你不在系统主页
3. 这个系统不是当前插件适配的页面

这时对应的典型报错是：

- `未找到主页面中的日报 iframe，请先进入包含“我的日报”的系统主页。`

### 7.3 看 iframe 能不能访问

```js
document.querySelector("#mainframe")?.contentDocument
```

如果是 `null` 或报异常，可能说明：

1. iframe 还没加载完
2. iframe 内容不可访问
3. 页面结构和预期不一致

### 7.4 看当前 iframe 是不是日报页

```js
const doc = document.querySelector("#mainframe")?.contentDocument;
!!doc?.querySelector("#start_time")
```

再看：

```js
!!doc?.querySelector("#search_btn")
```

如果这些都是 `false`，说明当前 iframe 还不是“我的日报”。

## 8. 第六步：检查是不是登录态问题

这个问题非常常见，因为顶层页面可能看起来没问题，但 iframe 里其实已经变成登录页。

在业务页 Console 里执行：

```js
const doc = document.querySelector("#mainframe")?.contentDocument;
!!doc?.querySelector("#password_input")
```

如果是 `true`，说明会话基本已经失效。

这类问题对应的插件报错通常是：

- `当前会话可能已失效，请先登录考勤系统后重试。`

如果用户说“我明明在主页”，你要特别警惕这种情况：  
顶层在主页，但 iframe 已经登录失效。

## 9. 第七步：检查是否成功切换到“我的日报”

插件现在会自动点击左侧“我的日报”。

如果报错：

- `自动打开“我的日报”失败，请确认当前页面为系统主页。`

优先检查：

1. 左侧链接是不是还存在
2. 链接 `href` 是否变了
3. iframe 刷新后是不是进入了别的页面

可以在 Console 里执行：

```js
Array.from(document.querySelectorAll("a")).filter((el) => (el.href || "").includes("my_report.jsp"))
```

如果这里都找不到，说明左侧菜单结构变了，插件的导航选择器需要更新。

## 10. 第八步：检查日报表单是否存在

插件进入日报页后，会找：

- `#start_time`
- `#end_time`
- `#search_btn`

手动检查：

```js
const doc = document.querySelector("#mainframe")?.contentDocument;
doc?.querySelector("#start_time")
doc?.querySelector("#end_time")
doc?.querySelector("#search_btn")
```

如果其中一个是 `null`，对应常见报错是：

- `未找到日报查询表单。`

这通常意味着页面结构变了，或者进入的不是预期页面。

## 11. 第九步：检查搜索后有没有刷新 iframe

插件触发搜索后，会等待 iframe `load`。

如果报错：

- `等待日报 iframe 刷新超时。`

常见原因：

1. 点击按钮后页面根本没发请求
2. 按钮事件逻辑变了
3. 页面请求太慢
4. iframe 没真正 reload

这个时候你要看业务页 F12 的 Network：

1. 点击插件统计按钮
2. 看是否出现 `my_report.jsp`

如果没有：

1. 说明点击没触发成功
2. 或者页面行为已经不是简单表单提交了

## 12. 第十步：检查是否拿到了完整表格

如果报错：

- `iframe 已刷新，但未找到日报表格。`
- `未找到日报表格，页面结构可能已变化。`

说明已经走到查询结果页了，但没找到：

```js
#maintable
```

你要在 Console 中验证：

```js
const doc = document.querySelector("#mainframe")?.contentDocument;
doc?.querySelector("#maintable")
```

如果是 `null`，说明：

1. 表格 ID 改了
2. 页面结构变了
3. 实际返回的不是结果页
4. 会话异常跳转了别的页面

## 13. 第十一步：检查网络请求到底是谁发的

这点非常关键。

### 页面发的请求

如果请求是业务页或 iframe 页面自己发的：

- 业务页 F12 Network 一般能看到

### background 发的请求

如果请求是扩展后台发的：

- 业务页 F12 看不到
- 要去插件的 `service worker` DevTools 看

### 当前插件主要链路

当前主链路是页面内驱动 + iframe 环境 fetch，因此你在业务页 Network 里通常应该能看到相关请求。

如果用户说“插件明明执行了，但 F12 看不到请求”，先确认：

1. 看的是不是业务页自己的 DevTools
2. 请求是不是 background 发的

## 14. 第十二步：检查解析结果是否异常

如果统计结果数值不对，但流程没有报错，重点看解析器。

### 14.1 总工时不对

先确认：

1. `用时(H)` 列名是不是还叫这个
2. 单元格内容是不是纯数字
3. 是否有空值、中文、异常格式

对应代码在：

- [lib/report-parser.mjs](/Users/piaochongmogu/VscodeProjects/chrome-plugin/lib/report-parser.mjs#L27)

### 14.2 只统计了一部分

先判断你读的是：

1. 完整 HTML
2. 还是当前页 DOM

如果以后再次出现“只统计第一页”，优先怀疑是：

1. 完整 HTML 获取失败
2. 解析器被改成了直接读当前 DOM

### 14.3 出勤天数不对

先确认 `日报日期` 列是否存在，并且日期格式是否仍为：

```text
YYYY-MM-DD
```

### 14.4 工时日历不对

重点检查：

1. `dailyHoursMap` 是否生成
2. 日期格式是否统一
3. 同一天多条记录是否已累加

## 15. 常见报错对照表

### `未找到主页面中的日报 iframe，请先进入包含“我的日报”的系统主页。`

含义：

- 当前页没有匹配到主 iframe

优先检查：

1. 当前是不是系统主页
2. `#mainframe` 还在不在
3. 页面结构是否变化

### `当前会话可能已失效，请先登录考勤系统后重试。`

含义：

- iframe 页面已经变成登录页

优先检查：

1. iframe 内是否有密码框
2. 用户是否真的重新登录成功

### `自动打开“我的日报”失败，请确认当前页面为系统主页。`

含义：

- 插件尝试自动导航，但没成功进入日报页

优先检查：

1. 左侧菜单链接是否变化
2. iframe 导航是否被拦截

### `未找到日报查询表单。`

含义：

- 找不到日期输入或搜索按钮

优先检查：

1. 选择器是否还有效
2. 当前 iframe 是否真的是日报页

### `等待日报 iframe 刷新超时。`

含义：

- 搜索动作后没有等到 iframe reload

优先检查：

1. 点击是否成功
2. Network 是否有请求
3. 页面对查询逻辑是否改版

### `iframe 已刷新，但未找到日报表格。`

含义：

- iframe 已变化，但结果页里没有 `#maintable`

优先检查：

1. 返回的是不是别的页面
2. 表格 ID 是否变化

### `未找到日报表格，页面结构可能已变化。`

含义：

- 解析 HTML 时没有找到结果表格

优先检查：

1. 原始 HTML 是否正确
2. 表格结构是否改动

### `页面通信失败: ...`

含义：

- background 和 content script 通信失败

优先检查：

1. content script 是否已注入
2. 页面是否刷新过

### `注入页面脚本失败: ...`

含义：

- `chrome.scripting.executeScript()` 失败

优先检查：

1. 当前 tab 是否是允许注入的业务页面
2. host 权限是否匹配

## 16. 建议保留的一组 Console 排查命令

以后别人电脑出问题，你可以让对方直接在业务页 Console 执行这组命令，把结果发给你。

```js
location.href
document.querySelector("#mainframe")
document.querySelector("#mainframe")?.contentDocument
document.querySelector("#mainframe")?.contentDocument?.querySelector("#start_time")
document.querySelector("#mainframe")?.contentDocument?.querySelector("#search_btn")
document.querySelector("#mainframe")?.contentDocument?.querySelector("#maintable")
document.querySelector("#mainframe")?.contentDocument?.querySelector("#password_input")
```

只看这几条结果，你通常就能判断：

1. 是否在正确页面
2. 是否存在 iframe
3. 是否已进入“我的日报”
4. 是否已经掉登录
5. 是否拿到了结果表格

## 17. 建议用户反馈时必须提供的信息

以后如果别人说“插件坏了”，你至少让对方给你这些：

1. Chrome 版本
2. 插件版本或提交号
3. 当前页面 URL
4. popup 报错截图
5. `service worker` Console 报错截图
6. 业务页 Console 报错截图
7. 上一节那组 Console 命令的结果

没有这些信息，很多问题只能猜。

## 18. 最高频的 5 个真实原因

根据这类插件的特点，最常见的实际原因一般是：

1. 插件重载了，但业务页没刷新，旧 content script 还在
2. 当前打开的不是系统主页
3. iframe 内已经登录失效
4. 页面结构变了，选择器失效
5. 表格结构变了，解析器失效

所以你排查时不要先怀疑“统计公式错了”，先怀疑：

- 页面是否对
- 上下文是否对
- 登录态是否对
- 结构是否对

## 19. 一句话排查模型

以后遇到问题，你可以先在脑子里走这 4 句：

1. 插件脚本有没有成功注入？
2. 当前是不是系统主页？
3. iframe 里是不是“我的日报”而不是登录页？
4. 最终拿到的是完整 HTML 还是只有当前页 DOM？

这 4 个问题一旦答清楚，绝大部分问题都能定位。
