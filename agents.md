# 背景
员工需要访问采购的内部考勤系统， 但并没有提供工时统计功能

# 参考页面与字段
由于安全限制， 我只能将所需内容 copy 出来，文件列表：
1. 总体页面.html 点击进入考勤系统的主页面
2. my_report.jsp页面  点击我的日报进入的页面， 这是需要统计的页面
3. 点击我的日报 my_report.jsp请求与响应  如标题， 该文件内容为选择日期， 然后点击 my_report.jsp后， 的请求与响应。
4. 固定统计 用时(H)  这一列作为工时
5. my_report.jsp是前端分页， 需要处理好分页， 讲查询到的全部工时统计


# 目标

1. 以chrome 插件形式， 提供上月工时统计， 当月工时统计， 两个按钮。 并在两个按钮下存在一个用来展示一个页面悬浮窗的按钮。  
2. 点击上月工时统计， 自动使用正确的时间， 请求 my_report.jsp并统计工时。
3. 点击当月工时统计，自动使用正确的时间， 请求 my_report.jsp并统计工时。
4. 当插件页面悬浮窗的按钮点击后页面以悬浮窗的形式展示功能插件，， 悬浮窗的页面展开后有三个按钮， 即上月， 当月，  以及执行统计。   且三个按钮上方有时间输入框， 用以方便的输入时间。  点击上月或当月， 讲时间带入输入框。 点击执行统计后统计工时。


# ai plan： Chrome 插件工时统计迭代规划
  ## Summary

  基于现有 my_report.jsp 页面行为，插件应优先走“复用已登录会话，直接请求
  POST /daily_report/my_report.jsp 并在前端解析结果”的方案，而不是模拟点击页面
  分页。因为页面本身是 DataTables 前端分页，服务端一次请求已返回完整表格数据，
  分页只是浏览器端渲染，统计时应直接汇总整张返回表中的“用时(H)”列。

  规划按“先打通链路，再完善交互，再补鲁棒性”推进，避免一开始把浮窗、按钮、时间
  输入和解析逻辑耦合到一起。

  ## Key Changes

  ### 迭代 1：确认请求链路并做最小可用统计

  - 创建 Manifest V3 插件骨架，至少包含 popup、background/service worker、
    content script 和权限声明。
  - 权限按最小集配置：
    activeTab、storage、scripting、目标内网域名的 host_permissions。
  - 明确统计数据源：
    直接请求 /daily_report/my_report.jsp，提交 start_time、end_time、search=搜
    索，复用当前登录态 Cookie。
  - 实现日期计算工具：
    支持“上月整月”和“当月截至今日”两种默认时间范围，统一输出 YYYY-MM-DD。
  - 实现最小统计逻辑：
    从响应 HTML 中解析 #maintable，定位表头“用时(H)”所在列，累计所有数据行的工
    时值并返回合计。
  - 最小交付物：
    popup 中先只做“上月统计”“当月统计”两个按钮和结果展示区，确保点击即可出结
    果。

  ### 迭代 2：补齐解析可靠性与异常处理

  - 解析逻辑不要写死列下标，必须通过表头文字“用时(H)”动态找列，避免列顺序变化
    导致统计错误。
  - 结果汇总时同时返回辅助信息：
    总工时、解析到的记录数、实际使用的开始/结束日期。
  - 加入异常分支：
    未登录或跳转登录页、请求失败、页面结构变化、表格为空、工时单元格非数值。
  - 定义统一错误提示：
    popup 内直接展示错误原因，不依赖浏览器控制台。
  - 明确分页处理策略：
    不操作 DataTables 的下一页按钮；直接解析响应 HTML 的全部 <tbody> 行，因为
    样例里 Showing 1 to 20 of 54 entries 说明完整数据已在 DOM 中。

  ### 迭代 3：加入悬浮窗入口与页面内操作面板

  - 在 popup 中增加“打开悬浮窗”按钮。
  - content script 注入页面内悬浮面板，面板包含：
    上月、当月、执行统计 三个按钮；
    开始时间、结束时间 两个输入框；
    结果展示区与加载状态。
  - 交互规则固定：
    点击“上月”自动填入上月起止日期；
    点击“当月”自动填入当月起始日至今日；
    点击“执行统计”按输入框当前值发起统计。
  - 悬浮窗默认可收起、可关闭；同一页面只允许存在一个实例，重复点击按钮时仅切换
    显示状态。

  ### 迭代 4：统一状态管理与用户体验

  - popup 与悬浮窗共用一套日期计算、请求、解析模块，避免两套统计逻辑分叉。
  - 使用 chrome.storage 保存最近一次统计结果和最近一次手动输入时间，便于 popup
    关闭后恢复状态。
  - 加入加载中、防重复点击、结果格式化展示。
  - 展示内容至少包含：
    时间范围、总工时、出勤天数 、记录条数、最后一次统计时间。
  - 若当前标签页不在目标考勤系统域名下，popup 明确提示用户切到系统页面后再打开
    悬浮窗。

  ### 迭代 5：收尾与发布前加固

  - 补充边界校验：
    开始时间晚于结束时间、跨月自定义范围、空输入、非法日期。
  - 验证不同月份场景：
    2 月、31 天月份、跨年 12 月到 1 月。
  - 检查插件在页面 iframe 场景下的适配。
    样例首页通过 iframe 加载 ./daily_report/my_report.jsp，需要明确悬浮窗是注
    入顶层页还是日报 iframe。
  - 整理最终交付：
    安装说明、权限说明、适用页面说明、已知限制。

  ## Public Interfaces

  - Popup UI
    两个固定统计按钮：上月工时统计、当月工时统计
    一个入口按钮：打开悬浮窗
  - 悬浮窗 UI
    两个日期输入框：start_time、end_time
    三个按钮：上月、当月、执行统计
  - 内部模块接口建议固定为：
    getPresetRange(type) 返回日期范围
    fetchReportHtml({ startDate, endDate }) 返回 HTML 文本
    parseWorkHours(html) 返回 { totalHours, rowCount }
    runStatistics(range) 返回完整统计结果对象

  ## Test Plan

  - 已登录状态下点击“上月统计”，能得到非空结果，且时间范围为上月 1 日到上月最
    后一日。
  - 已登录状态下点击“当月统计”，能得到非空结果，且结束日期为当天。
  - 悬浮窗点击“上月”“当月”时，输入框能正确回填日期。
  - 悬浮窗手动修改日期后点击“执行统计”，结果按输入值计算。
  - 样例分页场景下，统计结果覆盖全部记录，而不是仅当前第一页 20 条。
  - 表格为空时返回 0 工时和 0 记录，不报脚本异常。
  - 未登录、接口重定向、结构变化时，UI 给出明确错误提示。
  - 重复点击统计按钮时不会并发发送多次请求。

  ## Assumptions

  - 默认实现目标是 Chrome 插件，不考虑 Firefox 兼容。
  - 默认使用当前浏览器已登录的内网会话，不做账号登录能力。
  - 默认统计口径是直接累加表格“用时(H)”列，不额外过滤审核状态、项目分类或重复
    记录。
  - 默认“当月工时统计”取“当月 1 日到当前日期”，不是整月预测值。
  - 默认 my_report.jsp 的查询结果完整包含在单次响应 HTML 中，前端分页不需要额
    外翻页请求。
  - 若后续验证发现 iframe 内跨域或 Cookie 访问限制影响请求，再单独增加“由
    content script 在页面上下文发起请求”的兼容迭代。