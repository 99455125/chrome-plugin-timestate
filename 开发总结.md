# 日报工时统计插件开发总结

## 1. 项目目标

这个插件的目标不是“做一个新系统”，而是“在不改内网考勤系统代码的前提下，补上一套工时统计能力”。

核心能力有 4 个：

1. 在 Chrome 插件 popup 中提供 `上月工时`、`当月工时` 两个快捷统计按钮。
2. 在网页中提供一个可拖动的悬浮窗，支持输入日期并执行统计。
3. 自动进入“我的日报”页面、填充日期、触发搜索。
4. 正确统计全部数据，而不是只统计前端分页的第一页。

## 2. 需求分析怎么拆

表面需求是“求和”，但真正要拆成下面几件事：

1. 数据在哪里  
   数据在 `my_report.jsp` 页面里，不是单独开放给插件的 JSON 接口。

2. 数据怎么拿  
   页面是主页面 + iframe 结构，真正的日报页面在 iframe 里。

3. 用户怎么触发  
   既要支持 popup 快捷统计，也要支持网页中的悬浮窗操作。

4. 统计口径是什么  
   固定使用 `用时(H)` 这一列求和。

5. 还有哪些衍生统计  
   需要统计：
   - 总工时
   - 记录条数
   - 出勤天数
   - 每日工时，用于工时日历

6. 最大风险是什么  
   `my_report.jsp` 是前端分页，如果只读当前 DOM，会只拿到第一页。

所以这个项目的核心不是 UI，而是“如何稳定拿到完整日报 HTML 并正确解析”。

## 3. 技术方案为什么这样定

一开始最自然的想法是“插件直接请求 `my_report.jsp`”。但实际验证后发现，这种方案虽然有时能成功，但有两个问题：

1. 用户在业务页的 F12 Network 里看不到请求，调试体验差。
2. 如果完全走后台静默请求，页面没有真实变化，用户会怀疑插件到底有没有执行。

所以最终采用了两段式方案：

1. 先在页面里驱动真实操作  
   content script 定位 iframe，填 `start_time` 和 `end_time`，点击 `search_btn`，让页面真的刷新。

2. 再在 iframe 上下文里发一次同样的 POST  
   使用 iframe 自己的 `fetch` 请求完整 HTML，避免前端分页导致只统计第一页。

这个方案同时满足了：

- 用户体验：页面会真实刷新
- 会话复用：借用当前已登录页面环境
- 数据完整：解析原始 HTML，而不是解析分页后的 DOM

## 4. 插件架构

插件分成 3 层：

### 4.1 popup 层

文件：

- `popup.html`
- `popup.css`
- `popup.js`

职责：

- 提供按钮和结果展示
- 获取当前活动标签页
- 给 background 发送统计消息
- 打开网页悬浮窗

它不直接操作业务页面 DOM。

### 4.2 background 层

文件：

- `background.js`

职责：

- 统一接收 `RUN_STATISTICS`
- 解析预设日期范围
- 确保 content script 已注入
- 把统计任务转发给页面
- 调用解析器得到最终结果
- 保存最近一次统计结果

它像调度中心，不负责页面操作细节。

### 4.3 content script 层

文件：

- `content.js`

职责：

- 找到主页面中的 iframe
- 判断当前 iframe 是日报页、登录页，还是其他页
- 必要时自动点击左侧“我的日报”
- 填日期并点击查询
- 等待 iframe 刷新
- 获取完整 HTML
- 在页面中插入并维护悬浮窗

这层是真正和业务页面打交道的执行层。

## 5. 关键文件作用

### 5.1 `manifest.json`

作用：

- 声明这是一个 Chrome MV3 插件
- 配置 popup
- 配置 background service worker
- 配置 content script 注入规则
- 声明权限

关键配置：

- `activeTab`
- `storage`
- `scripting`
- `host_permissions`

### 5.2 `lib/date-range.mjs`

作用：

- 计算 `上月` 和 `当月` 的开始日期、结束日期

这个文件很小，但很重要，因为日期计算应该独立出来，不要散落在 UI 代码里。

### 5.3 `lib/report-parser.mjs`

作用：

- 解析日报 HTML
- 定位 `用时(H)` 列
- 定位 `日报日期` 列
- 统计总工时
- 统计记录数
- 统计出勤天数
- 聚合每日工时
- 检测登录页
- 处理无效工时单元格

这是整个项目里最核心的数据层。

## 6. 一次统计请求的完整流程

以点击 popup 中的“上月工时”为例：

1. 用户点击 popup 按钮。
2. `popup.js` 调用 `runPreset("lastMonth")`。
3. popup 获取当前活动标签页。
4. popup 向 background 发送 `RUN_STATISTICS` 消息。
5. background 用 `getPresetRange()` 计算时间范围。
6. background 调用 `chrome.scripting.executeScript()`，确保 `content.js` 已在页面中。
7. background 给当前 tab 发送 `RUN_STATISTICS_IN_PAGE`。
8. content script 在页面中寻找日报 iframe。
9. 如果当前不是“我的日报”，就自动导航到“我的日报”。
10. 找到 `#start_time`、`#end_time`、`#search_btn`。
11. 填入日期并点击搜索。
12. 等待 iframe 刷新完成。
13. 在 iframe 的页面上下文里发 POST 获取完整 HTML。
14. background 调用 `parseWorkHours()` 解析 HTML。
15. 结果写入 `chrome.storage.local`。
16. popup 渲染结果。

这个流程里，popup 不知道 iframe 怎么找，content script 不负责 UI 渲染，background 不直接碰 DOM。职责是分开的。

## 7. 为什么不能直接统计当前表格 DOM

这是项目里最容易踩坑的地方。

`my_report.jsp` 页面是前端分页。很多这类页面会把“全部数据”先放到 JS 里，再只把当前页行渲染到表格里。这样你看到的 `tbody tr` 只是当前页，不是所有页。

如果你直接写：

```js
document.querySelectorAll("#maintable tbody tr")
```

通常只能拿到第一页的记录。

所以正确策略是：

1. 让页面真实执行查询
2. 拿到查询响应的完整 HTML
3. 在完整 HTML 上做解析和统计

这就是为什么项目最终没有用“自动点下一页逐页统计”的方案。

## 8. 为什么要动态按表头找列

如果直接写死：

```js
const hoursCell = cells[7];
```

一旦后端加列、减列、调换顺序，插件就坏了。

所以解析器里采用的是：

1. 先读取表头
2. 找到文本为 `用时(H)` 的那一列
3. 再按这个索引读取每行工时

同理，出勤天数和工时日历也是通过 `日报日期` 列动态定位。

这是很典型的“反脆弱”写法。

## 9. 出勤天数和工时日历怎么做

### 9.1 出勤天数

出勤天数不是“记录条数”，因为同一天可能有多条日报。

所以解析时要：

1. 读取 `日报日期`
2. 放进 `Set`
3. 最后取 `Set.size`

### 9.2 工时日历

工时日历需要的是“每天总工时”，不是“每条日报工时”。

所以解析器里新增了 `dailyHoursMap`，格式类似：

```js
{
  "2026-03-02": 8,
  "2026-03-03": 12
}
```

生成方法是：

1. 遍历每一行
2. 读出该行日报日期
3. 读出该行工时
4. 同一天就累加

然后悬浮窗里的日历用这个结果渲染。

颜色规则：

- 工作日 8 小时：绿色
- 工作日其他工时：红色
- 工作日 0 小时：红色
- 周末 0 小时：不显示
- 周末有工时：红色

## 10. 为什么会出现“Receiving end does not exist”

这个错误的意思是：background 给页面发消息时，页面里根本没有可接收消息的 content script。

常见场景：

- 页面早就打开了，插件后来才重载
- content script 因为某些原因没注入成功

解决方法不是忽略错误，而是：

1. 先 `executeScript`
2. 再 `sendMessage`

这个修复现在放在 `background.js` 中。

## 11. 为什么未登录提示一开始不准

因为未登录时，顶层页面还在，但 iframe 里的内容已经是登录页。

如果你只判断“有没有 iframe”，会误以为用户还在系统主页。

正确做法是：

1. 找到 iframe
2. 进入 iframe 文档
3. 判断 iframe 里是不是登录页

也就是说，判断登录态要看“真正承载业务内容的文档”，不能只看顶层页面。

## 12. 悬浮窗为什么比较难

悬浮窗不是普通页面，而是“插进别人页面中的原生组件”。

它需要同时处理：

- 创建 DOM
- 注入 CSS
- 拖拽
- 定位
- 展开/收起
- 关闭
- 数据刷新
- 刷新后恢复
- 与页面搜索区共存，不挡住原页面

这比 popup 难得多，因为 popup 是独立页面，悬浮窗要适配现有业务页面。

## 13. 状态持久化是怎么做的

用了 `chrome.storage.local`。

主要存两类状态：

### 13.1 最近一次统计结果

键名：

- `lastStatistics`

作用：

- popup 打开后直接恢复最近一次统计结果

### 13.2 悬浮窗状态

键名：

- `floatingPanelState`

内容包括：

- 是否可见
- 开始日期
- 结束日期
- 是否收起
- 是否展开工时日历
- 最近一次结果
- 悬浮窗位置

这样刷新页面后，悬浮窗还能恢复到用户上次的状态。

## 14. 这个项目涉及的核心技术

你真正学到的技术点有这些：

1. Chrome Extension Manifest V3
2. popup / background / content script 三层结构
3. runtime message 和 tab message 通信
4. 动态注入 content script
5. `chrome.storage.local`
6. iframe DOM 操作
7. 表单填充和事件触发
8. `fetch` + `credentials: include`
9. HTML 解析
10. 数据聚合
11. 原生 JS 组件化思路
12. 页面状态恢复

## 15. 这次项目最值得记住的工程经验

### 15.1 先打通数据链路，再做 UI

如果一开始就沉迷做 popup 和样式，最后很可能发现数据根本不准。

正确顺序是：

1. 先确认请求
2. 再确认解析
3. 再做 UI

### 15.2 尽量不要写死页面结构

像列号、按钮位置、选择器都应该尽量动态匹配。

### 15.3 页面型插件的难点是“上下文”

你必须永远知道自己当前代码运行在哪：

- popup
- background
- content script
- iframe 页面

很多 bug 不是语法错，而是“你以为你在页面里，实际上你在 background”。

### 15.4 老系统开发最重要的是兜底

老系统经常会出现：

- 未登录
- iframe 为空
- 页面结构变化
- 分页行为和预期不一致

所以错误提示一定要明确，不能全都显示成“统计失败”。

## 16. 你现在应该具备的能力

如果你已经读懂这套实现，你应该能独立做这些事：

1. 增加一个新的快捷时间范围，比如“本周”。
2. 给工时日历增加点击某一天显示当天日志详情。
3. 把固定 host 改成配置项。
4. 给 popup 增加自定义时间范围输入。
5. 迁移到另一套结构相似的内网页面。

如果你做不到，通常不是代码能力不够，而是对“插件执行链路”还不够熟。

## 17. 建议的复习顺序

建议你按下面顺序再读代码：

1. `manifest.json`
2. `popup.js`
3. `background.js`
4. `content.js`
5. `lib/date-range.mjs`
6. `lib/report-parser.mjs`

读的时候重点问自己 3 个问题：

1. 这段代码运行在哪个上下文？
2. 它的输入来自哪里？
3. 它的输出交给谁？

只要这 3 个问题能答清楚，这个插件你就是真的懂了。
